using Verse;
using Void;

namespace PawnEditor;

public class SectionWorker_Ideology(SectionDef def) : SectionWorker(def)
{
    public override void OnLayout(Layout layout, Pawn pawn)
    {
        base.OnLayout(layout, pawn);
    }
}