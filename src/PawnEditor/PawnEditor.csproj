<?xml version="1.0" encoding="utf-8"?>
<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <RootNamespace>PawnEditor</RootNamespace>
        <AssemblyName>PawnEditor</AssemblyName>
        <ModName>PawnEditor</ModName>
        <TargetFramework>net472</TargetFramework>
        <LangVersion>latest</LangVersion>
    </PropertyGroup>
    <PropertyGroup>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
        <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
        <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    </PropertyGroup>
    <PropertyGroup>
        <OutputPath>bin\</OutputPath>
        <PlatformTarget>AnyCPU</PlatformTarget>
        <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    </PropertyGroup>
    <PropertyGroup Condition="'$(Configuration)' == 'Debug'">
        <DebugSymbols>true</DebugSymbols>
        <DebugType>full</DebugType>
        <Optimize>false</Optimize>
        <DefineConstants>DEBUG;TRACE</DefineConstants>
    </PropertyGroup>
    <PropertyGroup Condition="'$(Configuration)' == 'Release'">
        <DebugType>pdbonly</DebugType>
        <Optimize>true</Optimize>
        <DefineConstants>TRACE</DefineConstants>
    </PropertyGroup>
    <ItemGroup>
        <ProjectReference Include="..\Taffy\Taffy.csproj"/>
    </ItemGroup>
    <ItemGroup>
        <Publicize Include="Assembly-CSharp"/>
    </ItemGroup>
    <ItemGroup>
        <PackageReference Include="Krafs.Publicizer" Version="2.3.0">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Krafs.Rimworld.Ref" Version="1.6.4752-beta"/>
        <PackageReference Include="Lib.Harmony" Version="2.4.2"/>
    </ItemGroup>
    <ItemGroup>
        <Reference Include="HotSwap">
            <HintPath>..\..\..\..\..\..\..\Program Files (x86)\Steam\steamapps\common\RimWorld\Mods\HotSwap (1.6)\Assemblies\HotSwap.dll</HintPath>
        </Reference>
    </ItemGroup>
    <Target Name="Deploy" AfterTargets="Build">
        <!-- TODO: Make listing of runtime dll's less verbose -->
        <ItemGroup>
            <ModFiles Include="$(MSBuildThisFileDirectory)..\..\data\$(ModName)\**\*.*"/>
            <RuntimeDlls Include="$(OutputPath)PawnEditor.dll"/>
            <RuntimeDlls Include="$(OutputPath)PawnEditor.pdb"/>
            <RuntimeDlls Include="$(OutputPath)Taffy.dll"/>
            <RuntimeDlls Include="$(OutputPath)Taffy.pdb"/>
            <RuntimeDlls Include="$(OutputPath)HotSwap.dll"/>
            <RuntimeDlls Include="$(OutputPath)HotSwap.pdb"/>
            <RuntimeDlls Include="$(OutputPath)0Harmony.dll"/>
        </ItemGroup>
        <!-- Delete existing mod directory -->
        <RemoveDir Directories="$(RimWorldModsPath)\$(ModName)"/>
        <!-- Copy static mod files -->
        <Copy
                SourceFiles="@(ModFiles)"
                DestinationFiles="@(ModFiles->'$(RimWorldModsPath)\$(ModName)\%(RecursiveDir)%(Filename)%(Extension)')"
        />
        <!-- Copy built DLL into versioned assemblies folder -->
        <Copy
                SourceFiles="@(RuntimeDlls)"
                DestinationFolder="$(RimWorldModsPath)\$(ModName)\$(RimWorldVersion)\Assemblies"
        />
    </Target>
</Project>